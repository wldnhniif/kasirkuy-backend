#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

"$basedir/../../../../../../.npm-pwsh/powershell-7.0.3-win32-x64/pwsh.exe"   "$@"
exit $?
